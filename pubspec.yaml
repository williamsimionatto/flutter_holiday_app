name: flutter_holiday_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.0-210.3.beta <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  http: ^1.1.0
  equatable: ^2.0.3
  get: ^4.6.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
